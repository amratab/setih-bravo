<p id="notice"><%= notice %></p>

<p>
  <strong>Users:</strong>
  <% @project.users.uniq.each do |user| %><br>
                  <%= user.id %>
                  <%= user.email %><br>
                <% end %>
</p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @project.body %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
